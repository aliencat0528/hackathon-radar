> 繼承根目錄共用規則（Claude Code 已自動載入，勿重複讀取 ../CLAUDE.md）

# hackathon-radar — 開發規範差異

---

## 專案定位

黑客松賽事追蹤與通知。**核心是通知，不是資料庫**——
判斷「哪一場、什麼時候、該不該叫醒用戶」，而不是把所有賽事都收進來。

---

## Commit scopes

本專案專用（其餘沿用根規範）：

| scope | 用途 |
|-------|------|
| `watchlist` | `data/events.yaml` 追蹤清單增修 |
| `notify` | 通知管道與訊息模板 |
| `digest` | 通知內容三段式的產生邏輯 |
| `source` | 資料來源抓取與解析 |

---

## 決策編號

`HR-<三位數>`。規則繼承根 `prepare.md`，此處只記差異：

- 主線決策在本專案落地 → 只記落地細節 + `← D-00X`，**不複製理由**
- 本專案內部決策不上浮，除非影響其他子專案的通知機制

---

## 資料來源分級（硬規則）

抓進 `data/events.yaml` 的每個欄位都必須帶 `source_tier`：

| tier | 意義 | 可否作為通知依據 |
|------|------|-----------------|
| `official` | 主辦方官網／官方公告頁 | ✓ 可直接發通知 |
| `gallery` | Devpost／專案藝廊等結構化來源 | ✓ 可直接發通知 |
| `press` | 媒體報導、學校新聞稿 | ✓ 但訊息內需標來源 |
| `inferred` | 依歷屆時間推估 | ✗ **不得作為「報名開放」的觸發依據**，只能出現在「預估」欄位 |

**`inferred` 不得觸發通知**——這是本專案最容易犯的錯：
用去年的日期推今年，然後在錯誤的時間叫醒用戶，通知就失去可信度。
推估只用來決定「該在哪個月加密檢查頻率」。

---

## 通知訊息硬規則

每封通知固定三段，缺一段就不發（← `HR-002`）：

1. **今年比賽主旨** — 本屆主題與賽制變動
2. **去年得獎作品** — 至少 2 件，含功能描述
3. **今年趨勢變化** — 與去年相比什麼變了；**沒查到變化就寫「無明顯變化」，不得省略此段**

無法湊齊三段 → 降級為「僅日期提醒」並在信中明說缺哪一段，不假裝完整。
