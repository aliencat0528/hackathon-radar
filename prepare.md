# Prepare — hackathon-radar 決策記錄

> **版本**：HR-006 · 2026-08-14
> **載入時機**：討論本專案功能 / 通知機制 / 資料來源時。
> 記錄規則繼承根 `prepare.md`，本檔只寫差異與落地細節。

---

## 決策日誌（新的在上）

### HR-006 · 2026-08-14 · structure
- **決策**：本專案為**獨立 private repo**（← `D-002` polyrepo）。主 repo 只留規則層記錄
  （`prepare.md` D-025、`.gitignore` 一列、子專案索引一列）
- **private 的理由**：純自用通知工具，比照 `tab-secretary`（← `D-016`）的判斷。
  `events.yaml` 內含個人的參賽關注清單與判斷偏好，公開沒有好處。
  **可隨時翻成 public**——若後續 `source_tier` 分級設計要拿來當作品集談資，翻開即可；
  反方向（public → private）才是不可逆的，所以先取安全的一邊
- **棄選**：public（現在沒有要展示的東西，v0.1.0 連程式碼都還沒有）；
  併進主 repo（違反 D-002，且會讓子專案歷史永久混入規則層）
- **執行錯誤（記錄以免重演）**：本專案首次提交時被**誤 commit 進主 repo**。
  成因是只憑 `ls` 看到其他子專案也是目錄就類推，**沒有讀主 repo 的 `.gitignore`**——
  該檔有兩段都在列子專案。用戶指出後才發現，主 repo 分支已 force push 修正。
  詳見主 repo `D-025` 的「執行錯誤」與「未解」兩欄

### HR-005 · 2026-08-14 · scope
- **決策**：本次**只寫文件與資料契約，不寫實作程式碼**（比照 D-020／D-021／D-022 的立項慣例）。
  唯一的例外是**人工寄出 1～2 封樣本信**，用來驗證訊息格式讀起來的感覺
- **理由**：通知專案最貴的錯誤不是程式寫錯，是**訊息格式沒人想讀**。
  先用人工寄的樣本確認可讀性，再決定要不要為它寫抓取與排程
- **棄選**：直接寫完整排程 + 寄信服務（在格式還沒確認前就投入工程，格式一改全部重來）

### HR-004 · 2026-08-14 · data
- **決策**：`data/events.yaml` 每個日期欄位必須帶 `source_tier`（`official`／`gallery`／`press`／`inferred`），
  且 **`inferred` 不得觸發通知**，只能決定檢查頻率
- **理由**：2026-08 的賽事調查中，AWS AI Agent、AI CUP 秋季賽、Kaggle 5-Day 等場次的
  「9～11 月」全部是依 2025 年同期推估、主辦方尚未公布。
  若用推估日期發「報名開放了」，第一次錯就會讓後續所有通知失去可信度
- **手法沿用**：把來源設成 schema 必填 ← `D-021`；四態分級的**語意**與
  `customer-support-ops` 的 `confidence` 不同（那個分「怎麼來的」，本專案分「誰說的」），
  刻意不共用同一組值 ← 同 `D-022` 的判斷
- **棄選**：只存日期不存來源（省事，但無法區分「官方公布」與「我猜的」，等於把風險藏起來）

### HR-003 · 2026-08-14 · trigger
- **決策**：通知觸發條件是**報名開放／截止逼近**，不是固定週報
- **理由**：固定週報在沒有賽事的月份會變成噪音，噪音化之後就沒人看
  （← 同 `D-013` 4c、`D-023` 對「不製造紅燈」的同一判斷）。
  賽事的價值集中在「還來得及報名」這個窗口，錯過窗口的資訊沒有行動價值
- **檢查頻率隨月份調整**：9～11 月為台灣與國際賽事密集期，每週檢查；其餘每月
- **棄選**：固定每週寄（實作最簡單，但多數週沒有新事件）；只在截止前寄
  （來不及準備，賽前需要提前量）

### HR-002 · 2026-08-14 · content
- **決策**：通知訊息固定三段——**今年比賽主旨 + 去年得獎作品（至少 2 件含功能描述）+ 今年趨勢變化**。
  湊不齊就降級為「僅日期提醒」並明說缺哪一段
- **理由**：只給日期的提醒等於行事曆通知，用戶自己設鬧鐘就好。
  三段的價值在於**收到信當下就能判斷要不要投入**——主旨決定合不合適、
  去年作品決定水準線在哪、趨勢變化決定今年該往哪個方向想
- **「無明顯變化」也要寫**：省略該段會讓人以為是漏查。空結果與未查證必須可區分
  （← 同根 `CLAUDE.md`「查不了的標推測」的同一精神）
- **棄選**：只寄連結（把判斷成本丟回給用戶）；寄完整報告（太長，通知場景不會讀完）

### HR-001 · 2026-08-14 · channel
- **決策**：通知管道**先 email 驗證格式，主力目標為 LINE**。本階段只做 email 樣本
- **理由**：LINE Notify 已於 2025-03-31 終止服務，要走 LINE Messaging API——
  需自建 channel、webhook 與 access token，設定成本明顯高於 email。
  但用戶日常在 LINE、email 容易被略過，**長期主力仍是 LINE**
- **順序的取捨**：先 email 不是因為 email 比較好，是因為**這階段要驗證的是訊息格式而非通道**。
  格式定案後再接 LINE，可以避免格式改動時重接兩次
- **棄選**：直接做 LINE（設定成本前置，且格式未定就接通道，改格式要重測）；
  只做 email（用戶明說主力會是 LINE）；用第三方推播服務（多一個帳號與費用，自用規模不划算）

---

## 待討論事項

| # | 議題 | 卡在什麼 | 提出日期 |
|---|------|---------|---------|
| 1 | LINE Messaging API 的 channel 由誰持有 | 個人開發者帳號即可，但需確認免費額度是否足夠（推播則數限制），且 webhook 需要可公開存取的端點 | 2026-08-14 |
| 2 | 趨勢變化這段誰產生 | 需要跨年度比對，資料量小的時候人工即可；自動化要先有兩年以上的 `events.yaml` 快照 | 2026-08-14 |
| 3 | 是否納入 InnoServe 等「無公開作品說明」的賽事 | 這類賽事湊不齊三段（缺去年得獎作品的功能描述），依 `HR-002` 只能降級為日期提醒；要不要為此破例未定 | 2026-08-14 |
| 4 | email 樣本尚未實際寄出 | 目前的 Gmail 連線只有讀取權限，`send_message` 與 `create_draft` 皆回 Insufficient scope（需 `gmail.send` 或 `gmail.compose`）。樣本暫存於 `docs/samples/`，格式回饋先靠讀檔；要實際收信需用戶重新授權 | 2026-08-14 |

---

## 相關文件

- 追蹤清單資料契約 → `data/events.yaml`
- 通知訊息模板 → `docs/NOTIFICATION.md`
- 專案說明 → `README.md`
